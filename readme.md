# WebFetch Explorer

This tool is mainly aimed for developers during backend operations. This website allows API fetching with GET/POST/PUT/DELETE parameters and header/body optional params.

## Switch behavior

On -> Means that no parameters are accepted (only GET param).
Off -> Params are accepted.